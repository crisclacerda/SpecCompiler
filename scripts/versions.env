#!/bin/false
# scripts/versions.env — pinned dependency versions for SpecCompiler Core
# Sourced by: build.sh, Dockerfile (via ARG)
# DO NOT execute directly.

# ---------------------------------------------------------------------------
# Base image
# ---------------------------------------------------------------------------
DEBIAN_TAG="bookworm"

# ---------------------------------------------------------------------------
# Lua  (compiled from source — shared library for Pandoc system-lua)
# ---------------------------------------------------------------------------
LUA_VERSION="5.4.7"

# ---------------------------------------------------------------------------
# Pandoc  (compiled from source with system-lua for dlopen support)
# ---------------------------------------------------------------------------
PANDOC_VERSION="3.6.1"
GHC_VERSION="9.6.6"
CABAL_VERSION="3.10.3.0"

# ---------------------------------------------------------------------------
# Deno  (for compiling TypeScript utilities)
# ---------------------------------------------------------------------------
DENO_VERSION="2.6.4"

# ---------------------------------------------------------------------------
# PlantUML
# ---------------------------------------------------------------------------
PLANTUML_VERSION="1.2024.8"

# ---------------------------------------------------------------------------
# SQLite  (amalgamation, compiled as static library)
# ---------------------------------------------------------------------------
SQLITE_VERSION="3470200"
SQLITE_YEAR="2024"

# ---------------------------------------------------------------------------
# Native Lua C extensions
# ---------------------------------------------------------------------------
LSQLITE3_VERSION="v0.9.6"
LUV_TAG="1.48.0-2"
LUAZIP_TAG="v0.2.0"
AMATH_COMMIT="1b9e2f1ae31354fc62b8843b7c408edb4e33f3d3"

# ---------------------------------------------------------------------------
# Pure Lua libraries
# ---------------------------------------------------------------------------
DKJSON_VERSION="2.8"
SHA2_COMMIT="6adac177c16c3496899f69d220dfb20bc31c03df"
SLAXML_TAG="v0.8"
LUACOV_VERSION="0.15.0"
