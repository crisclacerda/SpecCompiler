# Inline Views Test @SPEC-VIEW-INLINE

## Direct Prefix Views

`lof:`

`lot:`

`toc:`

## Symbol Views

The symbol `symbol: SPECCOMPILER` represents SpecCompiler.

Use `symbol: API` for Application Programming Interface.

## Math Inline Views

The formula `math: E = mc^2` shows energy-mass equivalence.

Calculate `math: x = (-b +- sqrt(b^2 - 4ac)) / (2a)` for roots.

## Abbreviation Views

The `sigla: Application Programming Interface (API)` is used.

See `sigla: SpecCompiler (SDN)` for more details.
