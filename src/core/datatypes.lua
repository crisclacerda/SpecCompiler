-- src/core/datatypes.lua
-- Canonical datatype constants for spec_attribute_values.

return {
    STRING  = "STRING",
    INTEGER = "INTEGER",
    REAL    = "REAL",
    BOOLEAN = "BOOLEAN",
    DATE    = "DATE",
    ENUM    = "ENUM",
    XHTML   = "XHTML",
}
